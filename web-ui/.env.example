# web-ui 服务端环境变量（`npm run serve` / `npm run start` 读取 web-ui/.env）
# 使用：cp .env.example .env

CORE_API_URL=http://127.0.0.1:8203
CORE_ORG_ID=local
# 必须与根目录 .env 的 PORTAL_IDENTITY_SECRET 完全一致
PORTAL_IDENTITY_SECRET=change-me-portal-identity-secret
WEB_UI_PUBLIC_URL=http://127.0.0.1:8200
